# 后续计划

## 阶段 0：项目骨架

状态：已完成。

目标：

- 初始化 Git 仓库。
- 建立 Python-first 项目结构。
- 写入产品设计、实施计划、当前进度和后续计划文档。
- 创建最小计算测试，验证测试链路可用。

## 阶段 1：数据模型与迁移

状态：已完成。

目标：

- 使用 PostgreSQL 作为正式本地数据库。
- 建立 SQLAlchemy 模型。
- 建立 Alembic 迁移。
- 覆盖核心表：
  - fund_basic
  - fund_nav_daily
  - experiment_group
  - portfolio_experiment
  - portfolio_position
  - stress_period
  - backtest_result
  - backtest_nav_daily

## 阶段 2：CSV 净值导入

状态：已完成。

目标：

- 先支持手动 CSV 导入。
- 校验基金代码、日期、单位净值、累计净值、复权净值和数据来源。
- 为后续 AKShare 导入保留统一接口。

## 阶段 3：核心回测引擎

状态：已完成。

目标：

- 实现净值口径选择。
- 实现共同区间对齐。
- 实现缺失净值诊断。
- 实现不再平衡、月度再平衡、季度再平衡。
- 实现总调仓幅度、单边换手率和估算交易成本。
- 实现数据质量 A/B/C/D 分级。

## 阶段 4：实验组与报告

状态：已完成。

目标：

- 支持主实验、基准实验、变体实验。
- 支持压力区间诊断。
- 生成 Markdown 研究报告。
- 报告中展示净值口径、缺失数据、数据质量和防守资产诊断。

## 阶段 5：本地 Web UI

状态：已完成初版，待浏览器级验证。

目标：

- 基金池页面。
- 实验组列表。
- 创建组合实验页面。
- 回测看板。
- 防守资产诊断和报告页面。

## 阶段 6：真实数据 smoke 回测

状态：已完成。

目标：

- 用 fund-trace 导入的真实基金净值创建第一组 `75/15/10` 主实验。
- 将回测概要写入 `backtest_result`。
- 将日度组合净值写入 `backtest_nav_daily`。
- 自动生成 Markdown 研究报告。
- 验证报告语言保持研究工具边界，不写交易建议。

已完成结果：

- 主实验组合：45% A 股权益、30% QDII、15% 债券、10% 黄金。
- 回测区间：2021-07-06 至 2026-06-03。
- 日度回测净值：1185 条。
- 数据质量：A。

## 阶段 7：基准实验与人工校验

状态：待开始。

目标：

- 增加 `100% 权益/QDII` 基准。
- 增加 `75/25/0` 无黄金基准。
- 增加 `75/15/10` 不同再平衡频率对比。
- 选择一小段历史区间，用 Excel 手工计算对照组合日收益、净值、回撤。
- 校验净值口径、日期对齐、再平衡时点和成本口径。

## 阶段 8：前端浏览器验证

状态：待开始。

目标：

- 启动 API 和前端 dev server。
- 验证基金池、实验组、实验详情、回测看板、压力区间页面。
- 检查图表、Markdown 报告、表格布局和交互是否能完整走通。
